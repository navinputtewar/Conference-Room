<%= form_for(@conference_room) do |f| %>
  <% if @conference_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conference_room.errors.count, "error") %> prohibited this conference_room from being saved:</h2>

      <ul>
      <% @conference_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :available_booking %><br>
    <%= f.number_field :available_booking %>
  </div>

  <div class="field">
    <%= f.label :all_tags %><br>
    <%= f.text_field :tag_list, placeholder: "Tags separated with comma" %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>